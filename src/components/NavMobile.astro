<template>
    <nav class="navbar">
        <div class="logo">
            <img src="/public/logo.png" alt="Logo" />
        </div>
        <input type="checkbox" id="toggle" class="toggle-input" />
        <label for="toggle" class="toggle-label">&#9776;</label>
        <div class="menu">
            <ul>
                <li><a href="#">Inicio</a></li>
                <li><a href="#">Servicios</a></li>
                <li><a href="#">Nosotros</a></li>
                <li><a href="#">Contacto</a></li>
            </ul>
        </div>
    </nav>
</template>

<style>
    .navbar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1rem;
        background-color: #333;
        color: white;
    }

    .logo img {
        height: 40px;
    }

    .toggle-input {
        display: none;
    }

    .toggle-label {
        cursor: pointer;
        font-size: 1.5rem;
        margin-right: 1rem;
    }

    .menu {
        position: absolute;
        top: 100%;
        left: 0;
        background-color: #333;
        width: 100%;
        max-height: 0;
        overflow: hidden;
        transition: max-height 0.3s ease-out;
    }

    .toggle-input:checked + .menu {
        max-height: 300px; /* Altura máxima del menú desplegable */
    }

    .menu ul {
        list-style-type: none;
        padding: 0;
        margin: 0;
    }

    .menu ul li {
        padding: 0.5rem;
    }

    .menu ul li a {
        color: white;
        text-decoration: none;
        display: block;
        padding: 0.5rem;
    }

    .menu ul li a:hover {
        background-color: #555;
    }

    @media (min-width: 768px) {
        .toggle-input {
            display: none; /* Ocultar checkbox en pantallas más grandes */
        }
        .menu {
            position: relative;
            top: initial;
            max-height: initial;
            overflow: initial;
            background-color: transparent;
            width: auto;
        }
        .menu ul {
            display: flex;
        }
        .menu ul li {
            padding: 0 1rem;
        }
    }
</style>
